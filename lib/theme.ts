// Client-safe brand theme constant (no prisma / no static catalog imports), so
// it can be used from client components without pulling in server-only code.
export const NEUTRAL_THEME = { themeFrom: "#0A6640", themeTo: "#0d7a4d" };
